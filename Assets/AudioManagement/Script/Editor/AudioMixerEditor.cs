using System.Collections;
using System.Collections.Generic;
using UnityEditor;
using UnityEngine;

public class AudioMixerEditor : EditorWindow
{
    [MenuItem("Tools/AudioMixerEditor")]
    static void Init()
    {
        // AudioMixerEditor window = (AudioMixerEditor)EditorWindow.GetWindow(typeof(AudioMixerEditor));
        // window.Show();
        // Debug.Log("AudioMixerEditor");
    }



}
